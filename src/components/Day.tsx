import React, { FC } from 'react';
import {IToday} from '../helper/moment';

type Props = {
    day: IToday
}

export const Day:FC<Props> = ({day}) => {
    return (
        <>
        
        </>
    )
}